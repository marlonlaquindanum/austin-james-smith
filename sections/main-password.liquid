{% assign settings = section.settings %}
{% comment %}
<section
  class="password welcome"
  data-section-id="{{ section.id }}"
  data-section-type="password"
style="display:none"
  >

  <button class="password__login-button slider-button" data-js-toggle tabindex="0" title="{{ 'general.password_page.toggle' | t }}">
    {% render 'icon' with icon: 'lock', class: 'lock' %}
    {% render 'icon' with icon: 'close', class: 'close' %}
  </button>

  <div class="password__content" data-content="default">
    <header class="password__header" role="banner">
      <p class="ff-meta fs-meta c-meta">{{ shop.name }}</p>
      <h1 class="password__heading ff-heading fs-page-heading c-heading">{{ settings.heading }}</h1>
    </header>
    
    {% if shop.password_message != blank %}
      <p class="password__subheading fs-body fs-primary-body c-subheading">{{ shop.password_message }}</p>
    {% endif %}

    {% form 'customer' %}
      {% if form.posted_successfully? %}
        <div class="password__success fs-body-base c-body">
          {{ 'general.password_page.signup_form_success' | t }}
        </div>
      {% else %}
        {%- if settings.text != blank -%}
          <div class="password__text fs-body-base c-body-base">{{ settings.text }}</div>
        {%- endif -%}

        {{ form.errors | default_errors }}
        <div class="password__notify-form">
          <label class="visually-hidden" for="Email">Email</label>
          <input type="hidden" name="contact[tags]" value="prospect, password page">
          <input type="email"
            name="contact[email]"
            class="input input--round"
            id="Email"
            spellcheck="false"
            autocomplete="off"
            autocapitalize="off"
            placeholder="{{ 'general.password_page.signup_form_email_placeholder' | t }}"
          >
          <button class="btn btn--primary" type="submit">{{ 'general.password_page.signup_form_button_label' | t }}</button>
        </div>
      {% endif %}
    {% endform %}

    {%- if settings.show_share -%}
      <div class="password__share">
        {% render 'share-block' with url: shop.url %}
      </div>
    {%- endif -%}

  </div>

  <div class="password__content" data-content="login">
    <h3 class="password__login-heading ff-heading fs-heading-base c-heading">{{ 'general.password_page.login_form_heading' | t }}</h3>
    {% form 'storefront_password' %}
      {{ form.errors | default_errors }}

      <div class="password__login-form">
        <label class="visually-hidden" for="Password">Password</label>
        <input type="password"
          class="input input--round"
          name="password"
          id="Password"
          placeholder="{{ 'general.password_page.login_form_password_placeholder' | t }}"
        >
        <button class="btn btn--primary" type="submit">{{ 'general.password_page.login_form_submit' | t }}</button>
      </div>
    {% endform %}
    <p class="fs-body-base c-body">{{ 'general.password_page.admin_link_html' | t }}</p>
  </div>
</section>
{% endcomment %}

  <div class="hero-section">
        <div class="content">
            <h2>{{ section.settings.heading1 }}</h2>
            <h2>{{ section.settings.heading2 }}</h2>
            <h2>{{ section.settings.heading3 }}</h2>
            <!-- <p>Limited stock — get early access Thursday 08/08, sign up for password below</p> -->
            {% form 'storefront_password' %}
      {{ form.errors | default_errors }}

      <div class="password__login-form">
        <label class="visually-hidden" for="Password">Password</label>
        <input type="password"
          class="input input--round"
          name="password"
          id="Password"
          placeholder="Enter early access code here"
        >
        <button class="btn btn--primary" type="submit">{{ 'general.password_page.login_form_submit' | t }}</button>
      </div>
    {% endform %}
        </div>
    </div>



 <style>

 body, html {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
    font-family: Arial, sans-serif;
}

.hero-section {
    position: relative;
    width: 100%;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    text-align: center;
    background-size: cover;
    background-position: center;
    background-image: url('{{ section.settings.bg_1 | img_url:'master' }}'); /* Path to your desktop background image */
}

.hero-section .content {
    background-color: rgba(249, 247, 247, 0.52);
    padding: 20px;
    border-radius: 8px;
  width:38%;
}

   .errors{
     margin-left: -91px;
   }
.hero-section h1 {
    margin: 0;
    font-size: 2.5em;
  font-size: 45px;
    font-family: 'IBM Plex Sans', Helvetica, Arial, sans-serif;
    font-weight: bold;
    color: rgb(5, 5, 5);
    font-style: normal;
}

.hero-section h2 {
    margin: 2px 0;
 //   font-size: 1.5em;
  font-size: 2em;
    font-family: 'IBM Plex Sans', Helvetica, Arial, sans-serif;
    font-weight: bold;
    color: rgb(5, 5, 5);
    font-style: normal;
}

.hero-section p {
    font-size: 1em;
    margin: 20px 0;
  color:#000;
}

.hero-section form {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: 20px;
}

.hero-section input[type="password"] {
    /*padding: 10px;
    margin-bottom: 10px;
    border: none;
    border-radius: 30px;
    width: 80%;
    min-width: 300px;*/

      padding: 15px;
    margin-bottom: 10px;
       border: 1px solid rgba(0, 0, 0, 0);
    border-radius: 30px;
    width: 80%;
    min-width: 300px;
    font-size: 1em;
       background-color: rgb(255, 255, 255);
    color: #000;
  
}

.hero-section button {
    padding: 10px 20px;
    border: none;
    border-radius: 30px;
    background-color: #000;
    color: #fff;
    cursor: pointer;
}

.hero-section button:hover {
    background-color: #333;
}

@media (max-width: 768px) {
  .hero-section .content{
  padding:0px;
    width:90%;
  }
  
  .hero-section input[type="password"] {
    min-width:240px;
  }
    .hero-section {
        background-image: url('{{ section.settings.bg_2 | img_url:'master' }}'); /* Path to your mobile background image */
    }

    .hero-section h1 {
        font-size: 2em;
    }

    .hero-section h2 {
        font-size: 1.2em;
    }

    .hero-section p {
        font-size: 0.9em;
    }
}
  
 </style>  

{% schema %}
  {
    "name": "t:sections.main_password.name",
    "settings": [
      {
        "type": "header",
        "content": "Text Info"
      },
      {
        "id": "heading1",
        "type": "text",
        "label": "heading 1"
      },{
        "id": "heading2",
        "type": "text",
        "label": "heading 2"
      },{
        "id": "heading3",
        "type": "text",
        "label": "heading 3"
      },{
        "id": "bg_1",
        "type": "image_picker",
        "label": "Background Image Desktop"
      },{
        "id": "bg_2",
        "type": "image_picker",
        "label": "Background Image Mobile"
      },{
        "type": "header",
        "content": "Old Info"
      },{
        "id": "heading",
        "type": "text",
        "label": "t:sections.main_password.settings.heading.label",
        "default": "Opening Soon"
      },
      {
        "id": "text",
        "type": "richtext",
        "label": "t:sections.main_password.settings.text.label",
        "default": "<p>Find out when we open<p>"
      },
      {
        "id": "show_share",
        "type": "checkbox",
        "label": "t:sections.main_password.settings.show_share.label",
        "default": true
      }
    ]
  }
{% endschema %}
